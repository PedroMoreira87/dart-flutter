import 'package:flutter/material.dart';

class SaveWeightScreen extends StatelessWidget {
  const SaveWeightScreen({Key? key}) : super(key: key);
  static const routeName = '/save-weight';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
